import React, { Component } from 'react'
import { View, FlatList, TouchableOpacity, RefreshControl, Image, Alert, Dimensions, StyleSheet, Platform, Animated, Easing, } from 'react-native'

const { height, width } = Dimensions.get('window')

export default class NavBar extends Component {
    constructor(props){
        super(props)
        this.state = {  }
    }

    render(){
        return (
            <View>

            </View>
        )
    }


}